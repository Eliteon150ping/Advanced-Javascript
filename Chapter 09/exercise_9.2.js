console.dir(window.location);             // Finds the location of the current file
console.dir(window.location.protocol);    // Finds the protocol in the location of the current file
console.dir(window.location.href);        // Finds the href in the location of the current file

